spring:
  datasource:
    url: jdbc:h2:mem:movie_db;DB_CLOSE_DELAY=-1
  jpa:
    hibernate:
      ddl-auto: create
      properties:
        hibernate:
          dialect: org.hibernate.dialect.H2Dialect
